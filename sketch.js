let w = 900;
let h = 650;
let centreX = w * 0.5;
let centreY = h * 0.5;
/**
 * @type {Ball}
 */
let b;
/**
 * @type {Box}
 */
let box;

function setup() 
{
  createCanvas(w, h);
  print(degrees(PI/2));
  
}

function draw() 
{
  background(255);
  
}
